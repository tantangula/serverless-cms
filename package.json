{
  "name": "hello-world",
  "description": "Sample NodeJs web application",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.1"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "express-http-proxy": "^1.5.1",
    "mocha": "5.0.0",
    "unit.js": "2.0.0"
  },
  "scripts": {
    "start-front": "PORT=8080 react-scripts start",
    "start-back": "sam local start-api --host 172.31.28.5 --port 8081",
    "start-dev-proxy": "node proxy.js",
    "build": "react-scripts build",
    "test-front": "react-scripts test",
    "test-back": "mocha --recursive tests",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
